# 🚀 راهنمای سریع شروع کار

این راهنما برای **اجرای سریع پروژه** در کمتر از 5 دقیقه نوشته شده.

---

## ⚡ نصب و اجرا (5 دقیقه)

### 1️⃣ نصب pnpm (اگر نداری)

```bash
npm install -g pnpm
```

### 2️⃣ نصب dependencies

```bash
cd smartClubCRM
pnpm install
```

### 3️⃣ ساخت فایل‌های .env

```bash
# Venue App
cat > apps/venue-app/.env.local << EOF
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=supersecret123
NEXT_PUBLIC_API_URL=http://localhost:3001/api
EOF

# Web App
cat > apps/web-app/.env.local << EOF
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=supersecret123
NEXT_PUBLIC_API_URL=http://localhost:3000/api
EOF
```

### 4️⃣ اجرای Venue App

```bash
pnpm turbo dev --filter=@smartclub/venue-app
```

✅ باز کردن: http://localhost:3001/fa/login

---

## 👤 ورود به سیستم

**راه 1: دکمه‌های Quick Login**
- کلیک روی "مالک باشگاه" یا "مدیر" یا ...

**راه 2: ایمیل/پسورد**
- ایمیل: `venue-owner@test.com`
- پسورد: `123` (هر چیزی)

---

## 🎯 صفحات کلیدی

بعد از لاگین می‌تونی این صفحات رو تست کنی:

| صفحه | URL | توضیح |
|------|-----|-------|
| Dashboard | `/fa/overview` | نمای کلی با KPIها |
| تقویم | `/fa/calendar` | Gantt timeline |
| رزروها | `/fa/bookings` | لیست و مدیریت رزرو |
| امکانات | `/fa/assets` | زمین‌ها و امکانات |
| مشتریان | `/fa/customers` | CRM |
| کارکنان | `/fa/staff` | مدیریت تیم |
| گزارشات | `/fa/finance` | آمار مالی |
| قیمت‌گذاری | `/fa/pricing` | قوانین پویا |
| تنظیمات | `/fa/settings` | تنظیمات باشگاه |

---

## 🧪 تست سریع (2 دقیقه)

### ✅ تست 1: مشاهده Dashboard
1. لاگین کن
2. باید ببینی: 4 کارت KPI + لیست رزروها

### ✅ تست 2: ساخت رزرو جدید
1. برو `/fa/bookings`
2. کلیک "افزودن رزرو"
3. پر کن و ذخیره
4. ببین که توی لیست اضافه شد

### ✅ تست 3: تغییر زبان
1. برو `/en/overview`
2. همه چیز انگلیسی شد؟ ✅

---

## 🌍 تغییر زبان

```
فارسی: http://localhost:3001/fa/...
انگلیسی: http://localhost:3001/en/...
```

---

## 🔄 اجرای Web App (اگر بخوای)

```bash
pnpm turbo dev --filter=@smartclub/web-app
```

باز کردن: http://localhost:3000

**نکته:** Web App هنوز در حال توسعه است.

---

## 🛑 متوقف کردن

فقط `Ctrl+C` بزن.

---

## 🐛 مشکلات رایج

### ❌ Port 3001 قبلاً استفاده شده

```bash
# Kill کردن process روی port 3001
lsof -ti:3001 | xargs kill -9

# یا تغییر port در package.json:
"dev": "next dev -p 3002"
```

### ❌ pnpm command not found

```bash
npm install -g pnpm
```

### ❌ Build خطا می‌ده

```bash
# پاک کردن cache و node_modules
pnpm clean
rm -rf node_modules
pnpm install
```

---

## 📚 مستندات بیشتر

- **README کامل:** [README.md](README.md)
- **راهنمای تست:** [docs/TESTING.md](docs/TESTING.md)
- **معماری:** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- **وضعیت پروژه:** [docs/CURRENT-STATUS.md](docs/CURRENT-STATUS.md)

---

## 💡 نکات مهم

✅ همه authentication **mock** است (هر پسوردی کار می‌کنه)
✅ همه APIها **MSW** هستن (فرانت فقط)
✅ پروژه **فارسی RTL** + **انگلیسی LTR** داره
✅ Venue App **100% کامله** و آماده استفاده

---

**موفق باشی! 🎉**
